{
	"name": "clearcom-station-ic",
	"version": "1.0.3",
	"main": "dist/main.js",
	"type": "module",
	"scripts": {
		"postinstall": "husky",
		"format": "prettier -w .",
		"package": "yarn build && companion-module-build",
		"package-dev": "yarn build && companion-module-build --dev",
		"build": "rimraf dist && yarn build:main",
		"build:main": "tsc -p tsconfig.build.json",
		"dev": "tsc -p tsconfig.build.json --watch",
		"lint:raw": "eslint",
		"lint": "yarn lint:raw ."
	},
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/bitfocus/companion-module-clearcom-station-ic.git"
	},
	"engines": {
		"node": "^22.4"
	},
	"dependencies": {
		"@companion-module/base": "~1.13.2"
	},
	"devDependencies": {
		"@companion-module/tools": "^2.6.1",
		"@types/node": "^24.5.2",
		"eslint": "^9.36.0",
		"husky": "^9.1.7",
		"lint-staged": "^16.2.1",
		"prettier": "^3.6.2",
		"rimraf": "^6.0.1",
		"typescript": "~5.9.2",
		"typescript-eslint": "^8.44.1"
	},
	"prettier": "@companion-module/tools/.prettierrc.json",
	"lint-staged": {
		"*.{css,json,md,scss}": [
			"prettier --write"
		],
		"*.{ts,tsx,js,jsx}": [
			"yarn lint:raw --fix"
		]
	},
	"packageManager": "yarn@4.4.1"
}
